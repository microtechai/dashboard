# MIA — voz sin emojis y centro de operaciones

## Estado

Publicada y verificada en https://dashboard.microtechai.es el2026-09-08 23:26:29UTC /2026-09-09 00:26:29WEST. Seis archivos,18hashes remotos correctos (12protegidos intactos),38probes anónimos edge/origen: login200,root/index303,activos actuales/antiguos401. Backup `/root/mia-redesign-rollback-20260908T232629Z`; manifiesto privado `/home/ddr/mia-redesign-release/deployment-manifest.json`; versión pública `docs/evidence/mia/redesign/operator-preview.json`. Modalidad autorizada: **OPERATOR PREVIEW**, nunca aceptación de GPU física ni GO técnico global. Base real local/remota PR8 draft: `a90f51abf36663d80e3c488400da817101dcff13`, rama `feat/mia-reactor-integration`, apilada sobre `feat/jarvis-call-streaming`. 18 hashes vivos iniciales idénticos a la publicación anterior; cero drift de seguridad.

## Diagnóstico y cambios

- El normalizador anterior eliminaba solo siete badges al inicio de línea y explícitamente conservaba emoji semánticos. El backend enviaba texto sin filtrar a Piper. Ahora la única cola de lectura/auto-TTS elimina pictogramas extendidos, banderas regionales, modificadores de piel, secuencias ZWJ, variaciones FE0E/FE0F, tags y keycaps completos, en cualquier posición. La frontera autenticada `action=tts` repite esta defensa antes de invocar Piper. Solo emoji → no síntesis (backend400 explícito). No filtro de palabras «icono/symbols» ni nombres hablados inventados: no se proporcionó un ejemplo hablado exacto.
- Ejemplos comprobados: `Hola 👩🏽‍💻 🇪🇸 🏳️‍🌈 1️⃣ #️⃣ *️⃣ ❤️ ☀︎ 🫠 3.14 €` → `Hola 3.14 €`. Conserva dígitos normales, decimales, €, $, £, ¥, −5%, π, ×, ÷, ±, ∑, ∞, ≤, ≥, =, +, C++. No elimina HTML como si fuera DOM: presentación usa `textContent`, historial conserva el mensaje original exacto con emoji. Prueba real PHP/MC fixture verifica stdin del Piper fixture y persistencia intacta.
- Retirada la truncación silenciosa de Leer a32000 caracteres: se entrega el texto íntegro al buffer que ya informa error explícito si excede ese límite. El presupuesto anterior3bloques/respuesta y su aviso persistente siguen intactos, sin aumentar cuotas ni reintentar automáticamente.
- `voice/speech.js` legacy se conserva privado e idéntico en servidor, pero el índice ya no lo carga/instancia junto al chat. El antiguo loader solo diagnosticaba la instancia global; no era el controlador de llamadas moderno. El único propietario de captura y TTS es el chat autenticado; Hablar manual/Leer, auto-TTS en llamada, mute, barge-in/ruido, generación/cancelación y Sharvard speaker1/F no se reimplementan ni cambian de modelo.
- El núcleo estaba centrado enviewport720px aunque la barra lateral ocupaba220px. Reactor, barra y etapas usan ahora el mismo marco de contenido: **830px en1440px con sidebar abierta**, sin desplazamiento móvil; ResizeObserver coordina la geometría en el RAF existente. Sin otro renderer/RAF gráfico, sin copiar el prototipo ni tocar materiales/arcos de `reactor.js`.
- 17IDs/nodos conservados. El dashboard muestra contexto accesible agrupado en Servidores, Proyectos, IA/infraestructura; cada botón llama al `showNodeDetail` existente. Detalles cerrados por defecto. Las24entradas nav y módulos previos permanecen. Otras vistas conservan geometrías de catálogo en columnas estables por grupo, sin órbitas aleatorias. Esferas/radios no flotan encima del reactor; líneas de infra no simulan flujo de conversación.
- Cuatro etapas Entrada/STT/MIA/SalidaTTS observan exclusivamente los eventos reales `jarvis-chat-state`. Sin timers demo, progresoGPU, pasosRAG/tools ni conexionesMC inventadas. Contexto declara catálogo/no instrumentado. Chat conserva el inspector de flujo con estados/errores existentes.
- Acabado metal oscuro/azul eléctrico para nav, paneles, tipografía, espaciado, barra y botones. Iconos de navegación en SVG inline `aria-hidden`, sin emoji ni nombres de iconos enviados aTTS. Navegación legacy con equivalentes Enter/Espacio; contexto por teclado/Escape. Executor minimizado también en tablet para no tapar Enviar; preferencias desktop y mobile separadas.
- Dos problemas de interacción hallados con QA y corregidos: notificación conopacity0 interceptaba Salir (ahora no recibe puntero); Three r128 raycasteaba nodos invisibles y reabría un detalle al arrastrar el teatro (filtro explícito `mesh.visible`).

## Evidencia y rigor de QA

Directorio `docs/evidence/mia/redesign/`. Capturas reales aisladas PHP+formularioMC fixture, Chromium/SwiftShader, sin credencial de producción, sesión inyectada ni permiso de micrófono físico. Antes/después desktop1440×1120 y mobile390×844; después también tablet768×1024, chat/composer completos. Inspección visual confirma alineación y controles sin cobertura.

RED→GREEN observado para emojiFrontend, stdinPHP/historial, lectura sin truncación, centrado110px incorrecto, executor sobreEnviartablet, notificación sobreSalir, doble instancia legacy y guard de publicación. Pruebas dirigidas finales:18/18CallFrontend + navegador de navegación completo PASS antes de lanzar la última suite.

**Primera corrida abortada, no ocultada:** se detuvo voluntariamente con `process.kill` (SIGTERM), no OOM ni orden del usuario. El fixture de chat unía `?v=…` al nombre de fichero y devolvía404 a `speech.mjs`, causando siete timeouts de audio/límite. Se arregló solo el routerfixture para separar query, conservando avisos/aserciones/timeouts. El testfire heredado exigía cargar la vozlegacy: contrato actualizado a propietarioúnico, no eliminación del fichero. El navegador completo detectó el raycast invisible descrito arriba. Logs y motivo están en `initial-aborted/`; esa corrida NO demuestra suite completa. Después se comprobaron los fallos dirigidos y se lanzó una final con fuentes congeladas.

**Rendimiento:** baseline de la candidata desplegada anterior en el mismo navegador/viewport,30frames y readPixels,p95226.7ms,46drawcalls/30784triángulos. El muestreo final en `after-performance.json` dio p95202.5ms,13drawcalls/21664triángulos,crecimiento0; mejor que el baseline acotado pero NO aceptación hardware. Es un control acotado de regresión gruesa (límite fijo600ms), NO una aceptación técnica; el presupuesto250ms se reporta por separado, nunca se infla con un baseline dinámico. No nuevos ciclos de optimizaciónGPU, no umbral heredado `max(250,baseline*1.5+30)`. Incluso una muestra inferior250ms no certifica fluidez/GPU física. Framebuffer403200píxeles heredado y decisión históricaNO-GO intactos.

## Cierre de pruebas, sin reinterpretar fallos

La corrida completa final terminó **exit1:14/16comandos**, con CallFrontend18/18, frontend14, backend11, SpeechJS4+lectura1, stdinPHP1, branding2, reactorNode2, navegador24nav/17nodos, STTdecode y sintaxis PASS. El subrunner call terminó15/16: solo falló la exigencia obsoleta de instanciaLegacy; el otro fallo exterior fue el testexecutor a900px que esperaba escritorio abierto. **No se reejecutó toda la suite ni se cambiaron fuentes de producción tras ella.**

Dos contratos de test se corrigieron y ejercitaron: executor1280desktop/900tablet/390mobile con almacén fresco, restaurarEnter/click, minimizar/restaurar, borrador y preferencias separadas, ceroPOST; y barge-in nativo que exige ausenciaLegacy, conserva ONNX/Worklet/WebAudio/HTMLAudio, ruido/dobleinterrupción/RMS/cancelación. Revalidación exit0, native76.866s. `release-validation.json` verifica la unión de31entradas reales exitosas (14exteriores+15call+2rechecks), todos los hashes iguales antes/después; **31no significa pruebas nuevas ni nueva corrida completa verde**. `regressions.json` conserva14/16 y `call-regression/tests.json` conserva15/16. Logs exactos y vínculo SHA de las corridas anteriores impiden ocultar esas diferencias. Guard de publicación RED→GREEN cubre ausencia de flag, evidencia incompleta, fallos y drift; no se altera la decisión históricaNO-GO.

## Revisión Qwen real

Una petición existente `qwen3-coder-next` tras métricas running/waiting0, máximo1000tokens, fuenteSpeech y propuestaUI sin secretos; respuesta en `docs/evidence/mia/redesign-qwen.json`. Aprovechado: verificar móvil/centrado y defensabackend. Rechazado: llamar emoji aπ/°C/≈, whitelist numérica que mutilaría texto, afirmar ausencia de onError que sí existe, proponer no invalidar epochs/reintentar y falsa relación entre proxyMC y recuperaciónTTS. No revisión aprobatoria ni código copiado del modelo.

## Alcance de publicación

Seis archivos candidatos exactos: `index.html`, `chat/chat.css`, `chat/chat.js`, `chat/voice/speech.mjs`, `integration.js`, `api/chat.php`. **Sin activos nuevos, gate/allowlist sin cambio.** Nunca sync/delete. Backup selectivo fuera del webroot, hashes antes de cada replace y verificaciónposterior, sin Nginx/CF/servicios/modelos reiniciados. `server/deploy_redesign.py` exige `--operator-preview`,cobertura completa con resultados verdes y hashes de fuente estables antes deSSH (corrida más revalidaciones dirigidas, con procedencia y fallos originales intactos); cualquier auth/drift bloquea. Script anterior `deploy_mia.py` sigue dedicado a su entrega histórica, no reutilizarlo para esta.

MC real/subvenciones/proyectos/auditorías continúa separado e intacto. Los módulos 3D conservan placeholders explícitos, no constituyen CRUD/puente ni E2E deMC. Se preservan `api/stats.php`, legacy, configuración voz y archivosMC por hashes. Pendientes login/acústica real del propietario, mic/altavoces/AEC físico y estética/fluidez enGPU real. Sin checksCI publicados enPR8, no CI PASS implícito.
