# Microtech AI / MIA — desplegada para prueba del operador

## Estado vigente — 2026-09-08 22:36:15 UTC: OPERATOR PREVIEW

Autorización explícita posterior al NO-GO: **«despliegala para probarla»**. Publicada en https://dashboard.microtechai.es exclusivamente para prueba del propietario; **no es GO técnico ni PASS de rendimiento**. La decisión histórica `architectural-cycle.json` permanece byte a byte NO-GO. Hardware acelerado no disponible (QXL/SwiftShader; investigación local `/home/ddr/mia-hardware-qa/README.md`); no se repitió ni relajó el benchmark. Pendientes GPU física, estética/fluidez y audio/login/micrófono reales del dueño.

- Siete archivos exactos publicados con hashes idénticos a candidata y ledger final: index.html, login.html, chat/chat.js, chat/chat.css, integration.js, reactor/reactor.js y server/gate.php → `/opt/jarvis-access/gate.php`. Gate solo añade allowlist privada del reactor. 17 hashes iniciales sin drift; tras publicar, 18 rutas comprobadas (7 afectadas y 11 protegidas intactas). Sin sincronizar carpetas/borrado, reinicios, Nginx/CF/modelos ni cambios MC/API/voz/configuración. Voz Sharvard speaker1 y ruido se preservan por hashes, no nueva aceptación acústica.
- Backup selectivo completo de los seis archivos preexistentes afectados, fuera del webroot: `/root/mia-integration-rollback-20260908T223615Z`; nuevo reactor se revierte eliminando solo ese archivo. Manifiesto operativo privado local `docs/evidence/mia/deployment-manifest.json`, copia remota en backup; `python3 server/rollback_mia.py` dio **PASS CHECK ONLY**, NO aplicado. `--apply` requiere autorización de reversión. Nunca reabre login ni restaura todo el servidor.
- Verificación posterior: **36/36 probes** anónimos edge/origen: login200 con branding nuevo, raíz/index303, activos privados actuales/antiguos401. **18/18 hashes vivos** leídos de nuevo. No prueba autenticada falsa: acceso propietario y MC real siguen pendientes; 3D placeholders declarados, subvenciones/proyectos/audit separados e intactos.
- Controles dirigidos de esta publicación: 3 tests de decisión (default NO-GO bloquea, preview exige flag y registra hardware pendiente, fallo funcional bloquea preview), 2 branding, 2 contratos Node reactor, 14 frontend y 11 backend PASS; JS/PHP/Python sintaxis PASS. La suite completa previa conserva9/9comandos y llamadas16/16, 24 entradas/17 nodos; el PASS dinámico de render NO se acepta como rendimiento. No nueva suite GPU ni micro físico.
- `--operator-preview` está restringido al hash de decisión revisada, manifiesto exacto y ledger fuente estable/verde; no exime sintaxis, auth ni drift. Preflight HTTP se ejecutó antes del despliegue y quedó además incorporado al script para futuras ejecuciones. Se conserva el blanco EOF CSS por identidad con la fuente probada: advertencia de whitespace explícita, sin retoque de candidata.
- Evidencia pública saneada: `evidence/mia/operator-preview.json`; manifiestos operativos/baseline privado y snapshots históricos de configuración no se añaden a Git. El contenido inferior es historia previa, NO el estado actual de despliegue.


## Ciclo arquitectural final — NO-GO (prevalece sobre el histórico inferior)

Una sola optimización real: framebuffer limitado por viewport CSS a un presupuesto de 403200 píxeles, DPR acotado 0.5–1.5 y DPR de dispositivo. No cambia materiales, geometría, texto DOM, coordenadas de entrada, audio ni RAF. El perfil ahora sincroniza con `readPixels`: render mediano completo263.6ms, DPR.75=206.6ms, DPR.5=161.7ms; diagnóstico material unlit167.2ms (solo perfil, NO aplicado). Esto demuestra sensibilidad al coste de fragmentos/materiales, no un diagnóstico exclusivo de fill-rate ni rendimiento hardware. Perfil en `bounded-profile.json`.

**Móvil corregido y comprobado:** drawer y executor cerrados por defecto; toggles etiquetados/aria-expanded operables con Enter, expansión/restauración y compositor sin obstrucción. Preferencias executor móvil y desktop separadas, drawer mantiene estado por breakpoint. 17nodos y24entradas de navegación presentes; navegación completa y fallback GL de la corrida final PASS. Capturas reales `mobile-default.png`, `mobile-expanded-chat.png`, `after-desktop.png`, `after-desktop-angle.png` inspeccionadas: texto nativo legible; bordes3D desktop algo más suaves a DPR.5. No se afirma raster idéntico ni aprobación estética del dueño.

**Bloqueo exacto:** corrida dirigida tras la optimización p95 **292.4ms >290.85ms** del guard calculado. En la regresión completa el test dio PASS con candidata **275.8ms**, pero baseline358.8ms infló la fórmula heredada `max(250,baseline*1.5+30)` a **568.2ms**. No se modificó esa fórmula y no se usa ese verde permisivo para publicar: **275.8ms sigue >250ms**. Se incorporó `readPixels` a ambas mediciones; la suite anterior solo medía RAF, así que no se comparan directamente aquellos tiempos. Cero crecimiento de geometría,46drawcalls/30784triángulos. No más microajustes/repeticiones para obtener verde casual.

Decisión verificable en `evidence/mia/architectural-cycle.json`; ledger completo en `regressions.json`. **NO desplegado, NO commit/push/PR nueva**. Comprobación SSH solo lectura: **17/17 hashes producción iguales**, reactor ausente. Base/producción PR7 https://github.com/microtechai/dashboard/pull/7 SHA `a8aa284d22f17070d804705c5ce52d10d6da4570`. Gate/voz femenina/API/MC/Nginx/CF/modelos intactos. El script candidato ahora incluye también `integration.js` (siete archivos), pero no se ha ejecutado para publicar. Continúa el blancoEOF CSS histórico; fuente congelada durante QA.



**Cierre verificado:** `run_mia.py` exit0,9/9comandosPASS; `run_call.py` 16/16PASS. Fuentes congeladas y hashes actuales coinciden con ledger. RMS nativo112muestras, salida máxima0.2050, controles silencio/mutePASS; sin micro físico. NO-GO de publicación por el guard dinámico permisivo descrito arriba. Test adicional de seguridad RED→GREEN: `mia_release_decision_test.py`; `server/deploy_mia.py` ahora se niega antes de SSH si la decisión arquitectural es NO-GO, rechazo real verificado.16artefactos históricos call regenerados restaurados individualmente desdeHEAD tras verificar copia idéntica en `mia/call-regression`; ningún reset ni pérdida de cambios de usuario.

## Registro histórico de la candidata anterior

**NO desplegada, NO commit/push/PR nueva.** Rama `feat/mia-reactor-integration`, base PR7 `feat/jarvis-call-streaming`, HEAD/base `a8aa284d22f17070d804705c5ce52d10d6da4570`. Producción continúa en https://dashboard.microtechai.es con PR7. El manifiesto autoritativo es [candidate-manifest.json](evidence/mia/candidate-manifest.json): hashes candidatos, 17 hashes vivos preservados y ausencia del nuevo reactor en producción. Los scripts de publicación/reversión son candidatos, no ejecutados; no son prueba de despliegue.

## Implementación aditiva, todavía no liberable

- Port procedural del prototipo aprobado a THREE r128: metal oscuro, geometría anular, arcos coherentes y ramificados. Conserva `coreGroup`, renderer y propietario RAF existentes; no carga r170, no importa actividad/audio/textos simulados del prototipo. Sigue dibujando los 17 nodos originales.
- Branding visible del dashboard/login y etiquetas propias MIA; IDs, rutas, cookies, eventos anteriores e historial permanecen. `MIA, acrónimo de MicrotechAI`; no expansión alternativa. El prompt del servidor/API no fue modificado.
- Una sola conversación/compositor central reposicionada con CSS. Llamar, mute, colgar, Detener, Leer, Hablar manual, transcripción/historial, errores y advertencias no se sustituyen. Las vistas de módulos minimizan el teatro sin destruir la conversación. Se añadió acceso móvil explícito a métricas.
- RMS de entrada desde callbacks reales de captura y RMS de salida desde el analizador de reproducción existente, por canales separados. Transferencia visual documentada: `level=max(input,output)`; núcleo `emissive=1.3+level`, arco principal `power=.85+.5*level`. Expiración de muestras tras 1 s; no falsa telemetría.
- Flujo observable de micrófono, STT pendiente, petición/stream del modelo, reproducción y errores. Memoria/herramientas declaradas **no instrumentadas**. Obsidian solo como concepto/documentación: no se importaron datos privados.
- Aviso explícito en vistas estáticas: marcadores sin conexión a MC no son registros, importes ni resultados reales. Subvenciones/auditorías/informes del 3D siguen siendo placeholders; los módulos MC reales son separados y no se han conectado, migrado ni probado de extremo a extremo.
- Fallback de inicialización/pérdida WebGL conserva navegación/chat. Se conserva el gate y se propone únicamente añadir `reactor/reactor.js` a su allowlist privada; esta modificación NO está publicada.

## Bloqueo de publicación y límite de investigación

La prueba completa de navegador falla en el guard de renderizado, que **no se relajó**. Ver [render-performance.json](evidence/mia/render-performance.json) para la última corrida y [unbatched-performance.json](evidence/mia/unbatched-performance.json) para el estado previo.

Se hizo un perfil acotado con viewport 1440×1120, DPR1, SwiftShader y sin otra suite Chromium propia ejecutándose. [bounded-profile.json](evidence/mia/bounded-profile.json): reactor desactivado reduce la mediana de intervalo RAF aproximadamente de 201 a 108 ms; quitar arcos, nodos, overlays o luces no resuelve el coste. Media de `controller.update` ~0.346 ms. Media resolución reduce aproximadamente a 167 ms, insuficiente. `gl.finish()` devolvió tiempos de envío muy pequeños en este Chromium: **no se presenta esa cifra como tiempo GPU sincronizado**. El coste restante está asociado al render del reactor, pero no se atribuye a un cuello GPU exacto sin evidencia adicional.

Optimizaciones acotadas, sin alterar radios/anchos/profundidad/materiales: agrupación de piezas inmóviles por material y teselado proporcional al ángulo (las pequeñas bobinas no necesitan tantos segmentos como un círculo completo). Redujeron draw calls y triángulos. Una corrida posterior registró p95 292.2 ms frente a baseline132.5ms; la **última corrida completa registra p95 260.9 ms > 250 ms**, baseline117ms, 45draw calls/30,304triángulos y crecimiento0. La variabilidad no elimina el fallo; no se repite hasta conseguir un verde casual. El +1 de geometría observado en la corrida anterior no demuestra fuga: puede ser carga tardía por frustum y no aparece en la última muestra.

**Segundo pendiente visual:** en móvil, el drawer y executor inicialmente expandidos cubren parte del teatro. Los controles de cierre/minimización existen y el estado manualmente colapsado se verificó, pero no se declara aprobada la presentación móvil inicial. Las capturas actuales se inspeccionaron: geometría/arcos presentes, sin artefacto obvio por el teselado, pero esta obstrucción es visible. Se detuvieron los cambios arquitectónicos tras el perfil y la optimización acotada; NO despliegue de una candidata roja.

## QA: distinguir problemas y pruebas reales

- Branding, contratos Node/r128, navegación/inventario y layout tuvieron pasadas intermedias; las últimas corridas del navegador abortan antes de completar toda la navegación al fallar rendimiento. [navigation-matrix.json](evidence/mia/navigation-matrix.json) refleja la pasada intermedia, no una certificación de la última candidata.
- Fallo ambiental separado: `stt_worker_test.py` no encontraba PyAV en Python del sistema. Se creó únicamente `/home/ddr/mia-qa-venv`, con wheels fijados `av==16.0.1`, `numpy==2.3.3`. El test original de decodificación de 1/20/21 s pasó con ese intérprete. Runner selecciona `MIA_QA_PYTHON` o esa venv, comprueba importaciones y registra [qa-environment.json](evidence/mia/qa-environment.json). No se modificó el STT de producción ni se eludió PEP668.
- Fallo de fixture RMS separado: `HTMLAudio.muted=true` puede silenciar antes del analizador. Se usa un nodo Gain real a cero DESPUÉS del analizador. El siguiente fallo era una comparación de instantes diferentes: el predicate encontraba señal y la lectura posterior caía en una sección tranquila. Se sustituyó por serie acotada, emparejada en el update del RAF existente; no se bajó el umbral ni inventó RMS.
- **Prueba nativa independiente posterior PASS:** `JARVIS_HEADFUL=1 xvfb-run -a python3 tests/call_barge_in_test.py VoiceBargeIn`, 1 test, 77.890 s. Verifica WebAudio/ONNX/HTMLAudio reales con audio/API/login MC fixtures, 101 muestras visuales y 428 señales en esa corrida, salida máxima ~0.205, transferencia de uniforme en el mismo frame, salida cero tras terminar y entrada cero al silenciar. Conserva el doble barge-in, rechazo de ruido/golpes/tonos, timeout, permisos tardíos/denegados, cierre/logout. [audio-rms.json](evidence/mia/audio-rms.json) será actualizado por la corrida final: usar sus valores, no asumir constantes de la corrida anterior.
- La comprobación de whitespace `git diff --check` también detecta una línea vacía adicional al EOF de `chat/chat.css`. Se conserva la fuente congelada durante la corrida final; no se presenta como artefacto listo para commit. Los scripts nuevos de publicación/reversión/QA compilan, pero no se han ejecutado para publicar.
- Ningún permiso físico ni credencial real fue concedido/introducido. El audio de salida se silencia después del analizador. No prueba AEC físico, naturalidad, voz femenina o latencia de producción. La voz Sharvard speaker1/F privada activa y el auto-TTS de PR7 permanecen intactos; aceptación acústica/login reales del propietario siguen pendientes.
- [regressions.json](evidence/mia/regressions.json) y logs asociados son el ledger de la última suite completa. [initial-failed/](evidence/mia/initial-failed/) conserva la primera corrida roja. No se afirma suite verde ni CI PASS.

## Revisión Qwen real, no aprobación automática

[qwen-review.json](evidence/mia/qwen-review.json): endpoint existente, idle running/waiting 0/0, `qwen3-coder-next`, máximo1200 tokens, 28.79 s, respuesta limitada por longitud. Revisó código real anterior al batching, no la optimización final. Se rechazaron recomendaciones incorrectas: clonar índices JS no soluciona una supuesta fuga de BufferGeometry; uniforms numéricos no son recursos GPU independientes; el status ya tiene semántica live, el chat no debe convertirse en modal que bloquee navegación, y `aria-pressed` no corresponde a un párrafo de estado. La preocupación de layout sí se contrastó con capturas y bounds. No se presenta la revisión como aprobación del código ni se copiaron correcciones erróneas.

## Cierre de la última suite

`python3 tests/run_mia.py` terminó **exit1**: **8/9 comandos PASS**, falla únicamente el guard del navegador (p95260.9ms >250ms). Dentro de `tests/run_call.py`, **16/16 comandos PASS**; los logs contienen105 invocaciones de tests excluyendo chequeos de sintaxis (no se equiparan a recuentos históricos con distinta agregación). RMS final:98 muestras visuales/429 señales, pico de salida0.3254, controles negativos de silencio/mute PASS. Fuentes estables durante la suite; hashes en `regressions.json`. La matriz anterior conserva24 entradas de navegación/17 nodos, pero la navegación y las capturas multiángulo de la candidata final quedaron después del guard fallido y no se declaran completadas. La revisión de firmas del diff/untracked no encontró secretos; no es garantía exhaustiva.

Los artefactos generados por la regresión de llamadas se conservaron bajo `evidence/mia/call-regression/`; se restauraron únicamente los16 archivos históricos regenerados en `evidence/call/` desde HEAD, sin borrar tests ni planes. Ese snapshot también contiene manifiestos heredados de PR7: no representan un despliegue MIA. El único manifiesto de esta fase es `candidate-manifest.json`, `deployed:false`.

## Alcance preservado / siguiente decisión

Los 17 hashes vivos revisados no cambiaron, incluidos backend/API/configuración de voz, gate actual, scripts de voz/estadísticas exclusivos de producción y dependencias MC auditadas. No publicación, reinicio, Nginx/Cloudflare/modelos/servicios, CRUD/proxy o migración. Sin commit/push porque el criterio pedido exige PASS. Quedan cambios locales explícitos para revisión; no se eliminan plan, auditoría ni tests. El siguiente paso requiere decidir presupuesto/arquitectura visual y resolver la presentación móvil, no simplemente relajar el guard.
